<div class="container">
  <div class="lesson-content">
    <main class="content-prose">
      <div class="lesson-markdown lesson-markdown--chaptered">
        <slot />
      </div>
    </main>
  </div>
</div>

<style>
  .lesson-content {
    width: 100%;
  }

  .content-prose {
    max-width: 800px;
    margin: 0 auto;
    line-height: 1.78;
    color: var(--color-text);
    font-size: clamp(1.03rem, 0.99rem + 0.16vw, 1.1rem);
  }

  .content-prose :global(h2) {
    font-size: clamp(1.62rem, 1.45rem + 0.55vw, 1.9rem);
    color: #355046;
    margin: 2rem 0 1rem;
    font-weight: var(--font-weight-bold);
    border-bottom: 2px solid var(--color-border);
    padding-bottom: 0.5rem;
  }

  .content-prose :global(h3) {
    font-size: clamp(1.28rem, 1.18rem + 0.3vw, 1.45rem);
    color: #5f776d;
    margin: 1.5rem 0 0.75rem;
    font-weight: var(--font-weight-semibold);
  }

  .content-prose :global(h4) {
    font-size: 1.08rem;
    color: var(--color-text);
    margin: 1rem 0 0.5rem;
    font-weight: var(--font-weight-semibold);
  }

  .content-prose :global(p) {
    margin: 1rem 0;
    line-height: 1.82;
  }

  .content-prose :global(ul),
  .content-prose :global(ol) {
    margin: 1.5rem 0;
    padding-left: 2rem;
  }

  .content-prose :global(li) {
    margin: 0.5rem 0;
    line-height: 1.82;
  }

  .content-prose :global(li strong) {
    color: var(--color-primary);
  }

  .content-prose :global(strong),
  .content-prose :global(b) {
    color: var(--color-primary);
    font-weight: var(--font-weight-bold);
  }

  .content-prose :global(em),
  .content-prose :global(i) {
    font-style: italic;
    color: var(--color-text-secondary);
  }

  .content-prose :global(code) {
    background-color: var(--color-bg-secondary);
    color: var(--color-primary);
    padding: 0.25rem 0.5rem;
    border-radius: var(--radius-sm);
    font-family: 'Courier New', monospace;
    font-size: 0.95em;
  }

  .content-prose :global(pre) {
    background-color: var(--color-bg-secondary);
    border-left: 4px solid var(--color-primary);
    padding: 1rem;
    border-radius: var(--radius-md);
    overflow-x: auto;
    margin: 1.5rem 0;
    line-height: 1.5;
  }

  .content-prose :global(pre code) {
    background: none;
    color: inherit;
    padding: 0;
    border-radius: 0;
  }

  .content-prose :global(blockquote) {
    border-left: 4px solid var(--color-primary);
    padding: 1rem 1rem 1rem 1.5rem;
    background-color: var(--color-surface);
    margin: 1.5rem 0;
    border-radius: var(--radius-md);
    font-style: italic;
    color: var(--color-text-secondary);
  }

  .content-prose :global(a) {
    color: var(--color-primary);
    text-decoration: underline;
    transition: opacity var(--transition-fast);
  }

  .content-prose :global(a:hover) {
    opacity: 0.8;
    text-decoration: none;
  }

  .content-prose :global(table) {
    width: 100%;
    border-collapse: collapse;
    margin: 1.5rem 0;
    border-radius: var(--radius-md);
    overflow: hidden;
  }

  .content-prose :global(th) {
    background-color: var(--color-primary);
    color: white;
    padding: 0.75rem;
    text-align: left;
    font-weight: var(--font-weight-bold);
  }

  .content-prose :global(td) {
    padding: 0.75rem;
    border-bottom: 1px solid var(--color-border);
  }

  .content-prose :global(tr:last-child td) {
    border-bottom: none;
  }

  .content-prose :global(tr:nth-child(even)) {
    background-color: var(--color-surface);
  }

  .content-prose :global(strong + em) {
    display: block;
    margin: 0.5rem 0 1rem;
    padding-left: 1rem;
  }

  .lesson-markdown--chaptered {
    padding: clamp(1.35rem, 3.5vw, 2.3rem);
    border-radius: 1.6rem;
    background:
      radial-gradient(circle at top right, rgba(201, 111, 50, 0.12), transparent 12rem),
      radial-gradient(circle at top left, rgba(63, 92, 82, 0.08), transparent 14rem),
      linear-gradient(180deg, rgba(255, 255, 255, 0.99), rgba(249, 245, 237, 0.95));
    border: 1px solid rgba(24, 34, 48, 0.08);
    box-shadow: 0 24px 48px -32px rgba(24, 34, 48, 0.36);
  }

  .lesson-markdown--chaptered > :global(:first-child) {
    margin-top: 0;
  }

  .lesson-markdown--chaptered :global(.lesson-heading) {
    text-wrap: balance;
    scroll-margin-top: 1.2rem;
  }

  .lesson-markdown--chaptered :global(.lesson-heading-chapter) {
    margin: 2.4rem 0 1rem;
    padding: 1.1rem 1.15rem 0.95rem;
    border-radius: 1.1rem;
    border: 1px solid rgba(63, 92, 82, 0.16);
    border-left: 6px solid rgba(201, 111, 50, 0.92);
    background: linear-gradient(135deg, rgba(240, 246, 243, 0.98), rgba(255, 246, 236, 0.98));
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.78),
      0 10px 24px -24px rgba(24, 34, 48, 0.3);
    color: #27443c;
    font-size: clamp(1.52rem, 1.28rem + 0.58vw, 1.96rem);
    border-bottom: none;
  }

  .lesson-markdown--chaptered :global(.lesson-heading-section) {
    margin: 2.05rem 0 0.95rem;
    padding: 1rem 1.15rem 0.94rem 4.2rem;
    border-radius: 1.15rem;
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(244, 247, 246, 0.94));
    border: 1px solid rgba(63, 92, 82, 0.14);
    border-top: 4px solid rgba(63, 92, 82, 0.76);
    color: #2f4c43;
    font-family: var(--font-display);
    font-size: clamp(1.28rem, 1.08rem + 0.42vw, 1.58rem);
    font-weight: var(--font-weight-semibold);
    letter-spacing: -0.01em;
    line-height: 1.2;
    border-bottom: none;
    position: relative;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.82),
      0 16px 30px -30px rgba(24, 34, 48, 0.34);
  }

  .lesson-markdown--chaptered :global(.lesson-heading-section::before) {
    content: attr(data-section-number);
    position: absolute;
    top: 50%;
    left: 1.1rem;
    transform: translateY(-50%);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.25rem;
    height: 2.25rem;
    border-radius: 0.8rem;
    background: linear-gradient(135deg, rgba(201, 111, 50, 0.18), rgba(63, 92, 82, 0.16));
    color: #9b5627;
    font-family: var(--font-ui);
    font-size: 0.9rem;
    font-weight: var(--font-weight-bold);
    letter-spacing: 0;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.82),
      0 10px 20px -18px rgba(24, 34, 48, 0.34);
  }

  .lesson-markdown--chaptered :global(.lesson-heading-subsection) {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    margin: 1.5rem 0 0.7rem;
    color: #a2592b;
    font-family: var(--font-ui);
    font-size: clamp(0.98rem, 0.94rem + 0.16vw, 1.08rem);
    font-weight: var(--font-weight-semibold);
    letter-spacing: 0.05em;
    line-height: 1.35;
    text-transform: uppercase;
  }

  .lesson-markdown--chaptered :global(.lesson-heading-subsection::before) {
    content: '';
    width: 1.35rem;
    height: 2px;
    border-radius: 999px;
    background: linear-gradient(90deg, rgba(201, 111, 50, 0.92), rgba(63, 92, 82, 0.5));
    flex-shrink: 0;
  }

  .lesson-markdown--chaptered :global(.lesson-heading-feature) {
    margin: 1.6rem 0 0.7rem;
    color: #6a4b32;
    font-size: clamp(1.22rem, 1.12rem + 0.28vw, 1.42rem);
    font-style: italic;
    line-height: 1.28;
  }

  .lesson-markdown--chaptered :global(.lesson-heading-example) {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    margin: 1.4rem 0 0.55rem;
    padding: 0.34rem 0.72rem;
    border-radius: 999px;
    background: rgba(201, 111, 50, 0.1);
    border: 1px solid rgba(201, 111, 50, 0.2);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.72);
    color: #9f5829;
    font-family: var(--font-ui);
    font-size: 0.68rem;
    font-weight: var(--font-weight-semibold);
    letter-spacing: 0.09em;
    text-transform: uppercase;
  }

  .lesson-markdown--chaptered :global(.lesson-example-list) {
    margin-top: 0.35rem;
    padding: 0.95rem 1rem 0.95rem 1.3rem;
    border-radius: 1rem;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.99), rgba(245, 247, 241, 0.95));
    border: 1px solid rgba(63, 92, 82, 0.12);
    box-shadow: 0 16px 28px -28px rgba(24, 34, 48, 0.38);
  }

  .lesson-markdown--chaptered :global(.lesson-list-label) {
    margin-bottom: 0.55rem;
    color: var(--color-text-secondary);
    font-family: var(--font-ui);
    font-size: 0.78rem;
    font-weight: var(--font-weight-semibold);
    letter-spacing: 0.07em;
    text-transform: uppercase;
  }

  .lesson-markdown--chaptered :global(.lesson-labeled-list) {
    margin-top: 0.35rem;
    padding: 0.95rem 1rem 0.95rem 1.28rem;
    border-radius: 1rem;
    background: rgba(244, 247, 246, 0.8);
    border: 1px solid rgba(24, 34, 48, 0.07);
    box-shadow: 0 14px 26px -30px rgba(24, 34, 48, 0.32);
  }

  .lesson-markdown--chaptered :global(.lesson-labeled-list-ol) {
    list-style: none;
    margin-top: 0.35rem;
    margin-left: 0;
    padding: 0;
    counter-reset: article-step;
    display: grid;
    gap: 0.8rem;
  }

  .lesson-markdown--chaptered :global(.lesson-labeled-list-ol > li) {
    position: relative;
    margin: 0;
    padding: 0.9rem 1rem 0.9rem 3rem;
    border-radius: 1rem;
    background: rgba(255, 255, 255, 0.98);
    border: 1px solid rgba(24, 34, 48, 0.08);
    box-shadow: var(--shadow-sm);
    counter-increment: article-step;
  }

  .lesson-markdown--chaptered :global(.lesson-labeled-list-ol > li::before) {
    content: counter(article-step);
    position: absolute;
    top: 0.82rem;
    left: 0.95rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.55rem;
    height: 1.55rem;
    border-radius: 999px;
    background: rgba(201, 111, 50, 0.14);
    color: var(--color-accent);
    font-family: var(--font-ui);
    font-size: 0.78rem;
    font-weight: var(--font-weight-bold);
  }

  .lesson-markdown--chaptered :global(ul) {
    padding-left: 1.35rem;
  }

  .lesson-markdown--chaptered :global(ul li::marker) {
    color: var(--color-accent);
  }

  .lesson-markdown--chaptered :global(code) {
    background: rgba(63, 92, 82, 0.08);
    color: var(--color-primary-dark);
    border: 1px solid rgba(63, 92, 82, 0.08);
  }

  .lesson-markdown--chaptered :global(strong),
  .lesson-markdown--chaptered :global(b) {
    color: var(--color-primary-dark);
  }

  @media (max-width: 768px) {
    .content-prose {
      padding: 0 1rem;
      font-size: 1rem;
    }

    .content-prose :global(h2) {
      font-size: 1.42rem;
    }

    .content-prose :global(h3) {
      font-size: 1.2rem;
    }

    .content-prose :global(ul),
    .content-prose :global(ol) {
      padding-left: 1.5rem;
    }

    .lesson-markdown--chaptered {
      padding: 1.05rem;
      border-radius: 1.2rem;
    }

    .lesson-markdown--chaptered :global(.lesson-heading-chapter) {
      margin-top: 1.9rem;
      padding: 0.9rem 0.95rem 0.82rem;
    }

    .lesson-markdown--chaptered :global(.lesson-heading-section) {
      padding: 0.82rem 0.92rem 0.78rem 3.55rem;
    }

    .lesson-markdown--chaptered :global(.lesson-heading-section::before) {
      left: 0.9rem;
      width: 1.95rem;
      height: 1.95rem;
      border-radius: 0.72rem;
      font-size: 0.84rem;
    }

    .lesson-markdown--chaptered :global(.lesson-heading-subsection) {
      gap: 0.42rem;
      font-size: 0.94rem;
    }

    .lesson-markdown--chaptered :global(.lesson-heading-subsection::before) {
      width: 1.1rem;
    }

    .lesson-markdown--chaptered :global(.lesson-heading-example) {
      width: fit-content;
      max-width: 100%;
      flex-wrap: wrap;
    }

    .lesson-markdown--chaptered :global(.lesson-labeled-list-ol > li) {
      padding-left: 2.7rem;
    }
  }

  @media print {
    .content-prose {
      max-width: none;
      font-size: 11pt;
      line-height: 1.6;
    }

    .lesson-markdown--chaptered {
      padding: 0;
      border: none;
      border-radius: 0;
      background: none;
      box-shadow: none;
    }

    .lesson-markdown--chaptered :global(.lesson-heading-chapter),
    .lesson-markdown--chaptered :global(.lesson-heading-section),
    .lesson-markdown--chaptered :global(.lesson-heading-example),
    .lesson-markdown--chaptered :global(.lesson-example-list),
    .lesson-markdown--chaptered :global(.lesson-labeled-list),
    .lesson-markdown--chaptered :global(.lesson-labeled-list-ol > li),
    .content-prose :global(blockquote),
    .content-prose :global(pre) {
      background: none;
      box-shadow: none;
    }

    .lesson-markdown--chaptered :global(.lesson-heading-chapter),
    .lesson-markdown--chaptered :global(.lesson-heading-section) {
      border-radius: 0;
    }

    .content-prose :global(h2),
    .content-prose :global(h3),
    .content-prose :global(h4),
    .lesson-markdown--chaptered :global(.lesson-heading),
    .content-prose :global(blockquote),
    .content-prose :global(pre),
    .content-prose :global(table),
    .content-prose :global(li),
    .lesson-markdown--chaptered :global(.lesson-example-list),
    .lesson-markdown--chaptered :global(.lesson-labeled-list),
    .lesson-markdown--chaptered :global(.lesson-labeled-list-ol > li) {
      break-inside: avoid;
    }
  }
</style>
